name: release

on:
  push:
    tags:
      - 'v*.*.*'

permissions:
  contents: write
  packages: write

jobs:
  release:
    uses: benvon/reusable-workflows/.github/workflows/go-goreleaser-release.yml@cd087ac5ce4adfc261f319ac5df2545e02ec60cc
