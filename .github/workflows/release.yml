name: release

on:
  push:
    tags:
      - 'v*.*.*'

permissions:
  contents: write
  packages: write

jobs:
  release:
    uses: benvon/reusable-workflows/.github/workflows/go-goreleaser-release.yml@582850451229996e3bbf5d06a437dfcaa090701f
